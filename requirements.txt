# 核心框架
streamlit==1.29.0

# 数据处理
pandas==2.1.4
numpy<2,>=1.19.3
scikit-learn==1.3.2
joblib==1.3.2

# 化学信息学（RDKit）
rdkit-pypi==2022.9.5

# PyTorch 核心（使用官方预编译的 CPU 版本）
torch==2.1.2+cpu
torchvision==0.16.2+cpu
torchaudio==2.1.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# 图神经网络（与 torch 兼容的扩展，可按需安装具体版本）
# 使用预编译的wheel，兼容Python 3.10和CPU版本
--find-links https://data.pyg.org/whl/torch-2.1.0+cpu.html
torch-geometric==2.4.0

# 可视化与图像
matplotlib==3.7.5
pillow==10.1.0
plotly>=6.5.0

# 网络请求与工具
requests>=2.31.0

# 修复streamlit依赖版本冲突（精确锁定版本，放在前面）
rich==13.7.1
markdown-it-py<3