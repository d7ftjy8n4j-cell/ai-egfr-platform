# ========== 依赖冲突修复：必须放在最前面 ==========
# 严格锁定 streamlit 1.29.0 及其核心依赖版本，防止云端预装包导致冲突
rich==13.7.1
markdown-it-py==2.2.0
pygments==2.17.2
# 锁定 ipywidgets 版本，防止与已移除的 stmol 或未来其他包冲突
ipywidgets==7.6.3
# Streamlit 的隐含依赖，防止自动升级导致冲突
altair<6

# ========== 核心框架 ==========
streamlit==1.29.0

# ========== 数据处理 ==========
pandas==2.1.4
numpy==1.24.3
scikit-learn==1.3.2
joblib==1.3.2

# ========== 化学信息学（RDKit） ==========
rdkit-pypi==2022.9.5

# ========== PyTorch 核心（使用官方预编译的 CPU 版本） ==========
# 注意：以下三行是一个完整的安装指令，请保持顺序和换行
torch==2.1.2+cpu
torchvision==0.16.2+cpu
torchaudio==2.1.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# ========== 图神经网络 ==========
# 注意：此行指定了 torch-geometric 的查找链接，请保持单独一行
--find-links https://data.pyg.org/whl/torch-2.1.0+cpu.html
torch-geometric==2.4.0
torch-scatter==2.1.2
torch-sparse==0.6.18
torch-cluster==1.6.3
torch-spline-conv==1.2.2

# ========== 可视化与图像 ==========
matplotlib==3.7.5
pillow==10.1.0
plotly==5.18.0

# ========== 3D可视化 (使用稳定的py3Dmol，已移除stmol) ==========
py3Dmol==2.0.0.post2

<<<<<<< HEAD
# ========== 蛋白质-配体相互作用分析（可选）==========
# 注意：PLIP在某些严格限制的云端环境（如Streamlit Cloud）可能安装失败
# 如果安装失败，应用会自动进入降级模式（仅影响PLIP分析功能，不影响3D可视化）
# 如果云端环境持续安装失败，请在install脚本中跳过此包安装
# plip>=2.2.0
=======
# 修复streamlit依赖版本冲突（精确锁定版本，放在前面）
rich==13.7.1
markdown-it-py<3
>>>>>>> parent of baea5a3 (debug)

# ========== 网络请求与工具 ==========
requests==2.31.0

# ========== 其他工具 ==========
scipy==1.11.4
tqdm==4.66.1

